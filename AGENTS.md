# HD2 战术配装终端：Codex 项目指令

本文件是仓库级长期上下文。开始任何实质修改前，先阅读：

- `docs/PROJECT_CONTEXT.md`
- `docs/PRODUCT_REQUIREMENTS.md`
- `docs/DEVELOPMENT_STATUS.md`

如果代码现状与文档不一致，以代码和测试结果为事实，并在同一次修改中更新相关文档。

## 产品边界

- 产品名固定为“HD2 战术配装终端”。
- 这是面向简体中文玩家的免费、公开、非商业 Windows 10/11 x64 非官方粉丝工具。
- 技术栈固定为 Tauri 2、React、TypeScript、Vite；除非用户明确批准，不更换主技术栈或引入数据库。
- v0.1 聚焦单人配装；不加入小队分析、账号/云同步、游戏进程读取、悬浮窗、实时银河战争、社区投票、聊天式 AI、头盔或披风。
- UI 使用原创深色战术终端风格，不复刻官方 UI，不使用官方标志。
- 界面默认简体中文；装备显示中文名、英文名，并支持别名搜索。

## 用户已确认的开发约定

- 每次代码修改后不要生成安装包。只有用户明确下达“生成安装包”指令时，才运行 `pnpm tauri build` 或制作 NSIS 安装包。
- 普通验证优先运行 `pnpm test` 和 `pnpm build`；需要桌面联调时使用 `pnpm tauri dev`。
- 不把 API Key、密码、凭据、个人信息、本地缓存或诊断日志写入代码、配置、分享码、提交记录或测试夹具。
- 不提交 `node_modules`、`dist`、`src-tauri/target`、EXE/MSI、日志或 `.env` 文件。
- 不提交许可不明的 Wiki 或游戏内原图。只有取得明确再分发权利并记录来源、授权范围后，相关素材才能进入公开仓库或发行包；否则继续使用通用占位图形。
- 游戏事实数据在维护阶段人工交叉核对；应用运行时不依赖第三方社区 API。
- 数据修订由社区通过 Issue 提供来源，维护者核验后录入；不直接合并外部数据 PR。

## 实现规则

- 保持装备稳定 ID；分享码、收藏和评分规则依赖这些 ID。
- 战备分为轨道、飞鹰、蓝色（支援武器、背包、载具/机甲）、绿色（炮台、防御工事）四组。
- 配装包含护甲、主武器、副武器、手雷、4 个不可重复战备和 1 个被动。
- 本地规则评分是唯一分数来源。AI 只能解释已有评分和已有替换候选，不得新增、覆盖或重算任何分数。
- API Key 在桌面版中只允许存入 Windows Credential Manager。
- 网络端点只允许 HTTPS；仅 `localhost` 可使用 HTTP。
- 保持离线优先、零遥测和用户主动导出诊断日志的原则。
- 新增或调整装备事实时，记录来源 URL、核验版本和核验状态；无法确认的数据明确显示“待核验”，不要猜测。
- 可访问性要求包括键盘操作、可见焦点、非纯颜色状态提示、页面纵向滚动、五档字体大小和 1280×720 起的可缩放窗口。

## 质量检查

- 修改 TypeScript/React/CSS 后至少运行 `pnpm test` 和 `pnpm build`。
- 修改 Rust 后运行适用的 Cargo 检查；除非用户明确要求，不以打包安装程序作为验证手段。
- 修改目录或评分规则时，同时检查目录模式测试、评分确定性、上下限、封顶和黄金夹具。
- 修改分享码时覆盖往返、损坏校验和、未知装备、非法槽位、旧版本和 16KB 解码上限。
- 修改 AI 时覆盖超时、取消、非 JSON、未知候选、越权改分和密钥泄漏边界。
- 完成一项阶段性功能后，更新 `docs/DEVELOPMENT_STATUS.md` 的完成项、缺口和下一步。

## Git 协作

- 修改前检查 `git status`，保留用户已有或无关改动。
- 在功能分支上开发；不要未经授权直接提交或推送到 `main`。
- 暂存时只加入本次确认过的文件。
- 提交、推送、创建 PR 和发布都需要用户明确授权。

